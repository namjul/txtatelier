{
  "name": "@txtatelier/sync-invariants",
  "version": "0.0.0",
  "description": "Normative sync invariants: 3-way conflict detection, remote-change classification, shared by CLI + PWA",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^4.1.0"
  },
  "license": "MIT"
}
